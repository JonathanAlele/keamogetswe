[package]
name = "keamogetswe"
version = "0.1.0"
edition = "2021"
description = "Forward-only learning through friction in continuous media"

[dependencies]
ordered-float = "5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
